<%
  if(tableSelectors.hasRelationFields(table)) { 
  %><%- chunks.fieldsQueries({
        fields: fields,
        SchemaNameGenerator: SchemaNameGenerator,
        changeCase: changeCase,
    });
  %><%}; 

  if(tableSelectors.hasFileFields(table)) { %><%- chunks.fileField()%><% };

  if(tableSelectors.hasListFields(table)) { %><%- chunks.fieldsList()%><% }; 
  
  if(tableSelectors.hasAddressFields(table)) { %><%- chunks.addressField()%><% };
  
  if(tableSelectors.hasPhoneFields(table)) { %><%- chunks.phoneField()%><% }; 
%>