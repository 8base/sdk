import React from 'react';
import { Card, Heading } from '@8base/boost';

import { <%= changeCase.pascal(screenName)%>CreateDialog } from './<%= changeCase.pascal(screenName)%>CreateDialog';
import { <%= changeCase.pascal(screenName)%>EditDialog } from './<%= changeCase.pascal(screenName)%>EditDialog';
import { <%= changeCase.pascal(screenName)%>DeleteDialog } from './<%= changeCase.pascal(screenName)%>DeleteDialog';
import { <%= changeCase.pascal(screenName)%>Table } from './<%= changeCase.pascal(screenName)%>Table';

const <%= changeCase.pascal(screenName)%> = () => (
  <Card padding="md" stretch>
    <Card.Header>
      <Heading type="h4" text=" <%= changeCase.pascal(screenName)%>" />
    </Card.Header>

    <<%= changeCase.pascal(screenName)%>CreateDialog />
    <<%= changeCase.pascal(screenName)%>EditDialog />
    <<%= changeCase.pascal(screenName)%>DeleteDialog />
    <Card.Body padding="none" stretch scrollable>
      <<%= changeCase.pascal(screenName)%>Table />
    </Card.Body>
  </Card>
);

export { <%= changeCase.pascal(screenName)%> };
