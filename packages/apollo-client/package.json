{
  "name": "@8base/apollo-client",
  "version": "0.8.1",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "scripts": {
    "build": "yarn run build:clean && yarn run build:cjs && yarn run build:mjs && yarn run build:flow",
    "build:clean": "rm -rf ./dist && mkdir ./dist",
    "build:js": "babel src --ignore 'src/__fixtures__,**/__tests__'",
    "build:cjs": "yarn run build:js --env-name cjs --out-dir dist/",
    "build:mjs": "yarn run build:js --env-name mjs --out-dir dist/module && for file in $(find dist/module -name '*.js'); do mv \"$file\" `echo \"$file\" | sed 's/dist\\/module/dist/g; s/.js$/.mjs/g'`; done && rm -rf dist/module",
    "build:flow": "yarn flow-copy-source src dist",
    "docs": "NODE_ENV=test yarn doc src/index.js --section=EightBaseApolloClient"
  },
  "devDependencies": {
    "@8base/apollo-links": "^0.8.1",
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/plugin-proposal-class-properties": "^7.2.3",
    "@babel/plugin-proposal-object-rest-spread": "^7.2.0",
    "@babel/plugin-transform-flow-strip-types": "^7.2.3",
    "@babel/plugin-transform-runtime": "^7.2.0",
    "@babel/preset-env": "^7.2.3",
    "apollo-client": "^2.4.2",
    "apollo-link": "1.2.1",
    "babel-core": "^7.0.0-bridge.0",
    "babel-jest": "^23.6.0",
    "documentation": "^8.1.2",
    "flow-copy-source": "^2.0.2",
    "graphql": "0.13.0",
    "graphql-tag": "^2.10.0",
    "jest": "^23.6.0"
  },
  "dependencies": {
    "@babel/runtime": "^7.2.0",
    "apollo-cache-inmemory": "^1.2.10",
    "apollo-link": "1.2.1",
    "apollo-link-batch-http": "^1.2.2",
    "apollo-link-error": "^1.1.1"
  },
  "peerDependencies": {
    "@8base/apollo-links": "^0.6.1",
    "apollo-client": "^2.4.2",
    "graphql": "0.13.0",
    "graphql-tag": "^2.10.0"
  },
  "license": "MIT"
}
